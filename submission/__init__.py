"""Star Labs Track 4 submission package. The entry point is submission.agent.Agent;
re-exported here so `from submission import Agent` also works, whichever import
shape the official harness uses."""
from submission.agent import Agent  # noqa: F401
