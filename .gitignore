__pycache__/
*.py[cod]
.DS_Store
.env
results.json
data/catalog.jsonl
data/releases/
# Organizer-only code, private evaluation data, manifests, and build reports.
organizer/
secure/
# Internal provenance and release-audit working documents.
docs/audits/
docs/data_selection_audit.md
docs/participant_release_checklist.md
# Organizer-only pipeline tests.
tests/test_5core_builder.py
tests/test_organizer_pipeline.py
*.log

# Participant-kit catalogue archive (see DATA_ATTRIBUTION.md before redistributing).
data/catalog.jsonl.gz
# Generated suite artefacts: the frozen test definitions and every evaluation run.
# Regenerable from SUITE_VERSION + master seed; SHA256s recorded in the baseline
# manifests below, which ARE tracked.
results/*
!results/baselines/
results/baselines/*/*.py
